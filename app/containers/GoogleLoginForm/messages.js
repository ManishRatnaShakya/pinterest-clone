/*
 * GoogleLoginForm Messages
 *
 * This contains all the text for the GoogleLoginForm container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.GoogleLoginForm';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the GoogleLoginForm container!',
  },
});
